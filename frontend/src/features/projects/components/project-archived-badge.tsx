import { StatusBadge } from '@/design-system';

/** Badge indicating a project has been archived. */
export function ProjectArchivedBadge() {
  return <StatusBadge variant="neutral">Archived</StatusBadge>;
}
