databases:
  - name: agencyos-db
    plan: free
    databaseName: agencyos
    user: agencyos

services:
  - type: web
    name: agencyos-api
    runtime: docker
    plan: free
    dockerfilePath: ./backend/Dockerfile
    dockerContext: .
    healthCheckPath: /api/health/live
    envVars:
      - key: NODE_ENV
        value: production
      - key: AUTH_ENABLED
        value: false
      - key: DATABASE_URL
        fromDatabase:
          name: agencyos-db
          property: connectionString
      - key: CORS_ORIGIN
        sync: false
      - key: STORAGE_LOCAL_PATH
        value: /app/uploads
      - key: SWAGGER_ENABLED
        value: true
    disk:
      name: agencyos-uploads
      mountPath: /app/uploads
      sizeGB: 1
